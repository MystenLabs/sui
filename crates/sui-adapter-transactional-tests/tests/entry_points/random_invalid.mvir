// Copyright (c) Mysten Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

//# init --accounts A B --addresses test=0x0

//# publish --sender A
module 0x0.m {
    import 0x2.random;

    entry entry_random_mut(c: &mut random.Random) {
        label l0:
        abort 0;
    }

    entry entry_random_owned(c: random.Random) {
        label l0:
        abort 0;
    }

    public public_random_mut(c: &mut random.Random) {
        label l0:
        abort 0;
    }

    public public_random_owned(c: random.Random) {
        label l0:
        abort 0;
    }

}

//# set-address test object(1,0)

//# programmable --sender A --inputs immshared(8)
// attempting to take imm random as mut
//> test::m::entry_random_mut(Input(0))

//# programmable --sender A --inputs immshared(8)
// attempting to take imm random as owned
//> test::m::entry_random_owned(Input(0))

//# programmable --sender A --inputs immshared(8)
// attempting to take imm random as mut
//> test::m::public_random_mut(Input(0))

//# programmable --sender A --inputs immshared(8)
// attempting to take imm random as owned
//> test::m::public_random_owned(Input(0))

//# programmable --sender A --inputs mutshared(8)
// attempting to take imm random as mut
//> test::m::entry_random_mut(Input(0))

//# programmable --sender A --inputs mutshared(8)
// attempting to take imm random as owned
//> test::m::entry_random_owned(Input(0))

//# programmable --sender A --inputs mutshared(8)
// attempting to take imm random as mut
//> test::m::public_random_mut(Input(0))

//# programmable --sender A --inputs mutshared(8)
// attempting to take imm random as owned
//> test::m::public_random_owned(Input(0))
