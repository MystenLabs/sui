{
  # Select the key and value of the protocol configuration
  prot_cfg_key_value: protocolConfig {
    configs {
      key
	  	value
    }
  }
  # Select only the key of the protocol configuration
  prot_cfg_key_only: protocolConfig {
    configs {
      key
    }
  }

  # Select only the value of the protocol configuration
  prot_cfg_value_only: protocolConfig {
    configs {
      value
    }
  }
  
  # Select the key and value of the feature flag
  feat_flag_key_value: protocolConfig {
    featureFlags {
      key
	  	value
    }
  }
  
  # Select only the key of the feature flag
  feat_flag_key_only: protocolConfig {
    featureFlags {
      key
    }
  }

  # Select only the value of the feature flag
  feat_flag_value_only: protocolConfig {
    featureFlags {
      value
    }
  }
}
