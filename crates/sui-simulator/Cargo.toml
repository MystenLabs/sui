[package]
name = "sui-simulator"
version = "0.7.0"
authors = ["Mysten Labs <build@mystenlabs.com>"]
license = "Apache-2.0"
publish = false
edition = "2021"

[target.'cfg(msim)'.dependencies]
msim = { path = "../../../msim-fork/msim" }
