// Copyright (c) Mysten Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

//# publish
// allowed, no object is being made with the UID
mvir 0x0::m {
    import 0x2::object;

    public struct Foo has key {
        id: object::UID,
    }

    fun foo(f: Self::Foo): object::UID {
        let id: object::UID;
        label l0:
        Foo { id } = move(f);
        return move(id);
    }

}
