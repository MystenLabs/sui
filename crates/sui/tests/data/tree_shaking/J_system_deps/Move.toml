[package]
name = "J_system_deps"
edition = "2024.beta"

[dependencies]
# this is relative to the temp directory where this whole tree shaking dir is copied to
Sui = { local = "../../system-packages/sui-framework", override = true }

[addresses]
j_system_deps = "0x0"

