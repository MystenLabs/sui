---
title: Packages
description: >-
  A Move package on Sui includes one or more modules that define the package's
  interaction with onchain objects. You develop the logic for those modules in
  Move, compile them into an object, and publish that package object to a Sui
  network.
keywords:
  - move packages
  - move
  - packages
  - move smart contract
  - package overview
pagination_prev: null
goal:
  description: >-
    Reader gets a clear overview of packages and knows which subtopic to read
    next
  requires:
    - has_frontmatter:
        - title
        - description
    - pattern: '\]\(/[^)]+\)'
      min: 1
      label: Links to child pages
---

<AgentPrompt
  prompt={"Prepare this package for Mainnet publishing: verify tests, dependencies, addresses, upgrade policy, gas requirements, signer/custody plan, and produce a launch checklist."}
/>

A Move package on Sui includes one or more modules that define the package's interaction with onchain objects. You develop the logic for those modules in Move, compile them into an object, and publish that package object to a Sui network. 

import DocCardList from '@theme/DocCardList';

<DocCardList />
