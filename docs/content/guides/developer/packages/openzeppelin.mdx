---
title: OpenZeppelin Packages
description: OpenZeppelin contracts for Sui provide two packages, integer math and access. 
keywords: [ openzeppelin, oz, integer math, access, openzeppelin_math, openzeppelin_access ]
---

The OpenZeppelin contracts for Sui provide two packages for developing Move applications:

- [`openzeppelin_math`](https://docs.openzeppelin.com/contracts-sui/1.x/api/math): Deterministic arithmetic, decimal scaling, and configurable rounding.

- [`openzeppelin_access`](https://docs.openzeppelin.com/contracts-sui/1.x/api/access): Ownership-transfer wrappers for privileged key store objects.

## `openzeppelin_math`

Use the `openzeppelin_math` package when you need arithmetic behavior that is predictable, auditable, and safe around overflow or precision boundaries. 

### Example 

<ImportContent
	source="/content/contracts-sui/1.x/math.mdx"
	mode="code"
	language="move"
	org="OpenZeppelin"
	repo="docs"
	lines="29-38"
	noComments
/>

[Learn more in the OpenZeppelin docs](https://docs.openzeppelin.com/contracts-sui/1.x/api/math).

## `openzeppelin_access`

Use the `openzeppelin_access` package when direct object transfer is too permissive for your use case. It provides explicit transfer workflows that are easier to review, monitor, and constrain.

### Example 

<ImportContent
	source="/content/contracts-sui/1.x/access.mdx"
	mode="code"
	language="move"
	org="OpenZeppelin"
	repo="docs"
	lines="31-45"
	noComments
/>

[Learn more in the OpenZeppelin docs](https://docs.openzeppelin.com/contracts-sui/1.x/api/access).
