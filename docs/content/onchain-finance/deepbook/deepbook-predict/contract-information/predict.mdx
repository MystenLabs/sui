---
title: Predict
description: >-
  Reference for the expiry_market module: the ExpiryMarket shared object,
  pricer loading, mint quotes, minting, live and settled redemption,
  permissionless settlement, read accessors, error codes, and order events.
keywords:
  - expiry market
  - deepbook predict
  - mint_exact_quantity
  - mint_exact_amount
  - redeem_live
  - redeem_settled
  - try_settle
  - pricer
  - mint quote
  - order events
goal:
  description: >-
    Reader can learn the ExpiryMarket shared object, the pricing and quoting
    calls, the mint and redeem functions, settlement, and the emitted order
    events
  requires:
    - has_frontmatter:
        - title
        - description
        - keywords
      label: Has required frontmatter fields
    - min_words: 300
      label: Needs more content depth
    - has_questions: true
      label: Needs questions for AI search visibility
    - has_answer: true
      label: Needs answer summary for AI citation
questions:
  - What is an ExpiryMarket in DeepBook Predict?
  - How do I mint a DeepBook Predict position onchain?
  - Why does a DeepBook Predict transaction abort with EWrongPricer?
  - How does a DeepBook Predict market settle?
answer: >-
  DeepBook Predict trading lives in the deepbook_predict::expiry_market module.
  One shared ExpiryMarket object holds each expiry's cash, exposure index, and
  pause flag. You load a transaction-local Pricer with load_live_pricer, size
  the trade with quote_mint, then call mint_exact_quantity, mint_exact_amount,
  redeem_live, or redeem_settled with an account Auth. Settlement is the
  permissionless try_settle.
last_verified: 2026-09-02
verified_against: predict-testnet-8-21
---

Trading on DeepBook Predict runs through the `deepbook_predict::expiry_market` module. One shared `ExpiryMarket` object exists per underlying and expiry, and it owns that expiry's DUSDC custody, strike exposure index, congestion estimator, and mint pause flag. Every call on this page is a `public fun` that you invoke from a programmable transaction block with a `moveCall`. Several of them return values that the same transaction must consume, so plan the whole flow as one block.

The module lives at [`packages/predict/sources/expiry_market.move`](https://github.com/MystenLabs/deepbookv3/blob/predict-testnet-8-21/packages/predict/sources/expiry_market.move) on the `predict-testnet-8-21` branch. For the deployed package and object IDs, see [Contract information](/onchain-finance/deepbook/deepbook-predict/contract-information).

:::caution

DeepBook Predict runs on Sui Testnet only. There is no Mainnet deployment. The smart contracts might change, so treat the package IDs, object layouts, and function signatures on this page as Testnet integration targets pinned to the `predict-testnet-8-21` branch.

:::

## The ExpiryMarket shared object {#expiry-market}

`ExpiryMarket` has the `key` ability and is shared when the registry creates it, so any transaction can name it as an input. Authorization comes from the arguments a function demands, not from object ownership.

| **Field** | **Type** | **Description** |
|---|---|---|
| `id` | `UID` | Object ID of the shared market. |
| `propbook_underlying_id` | `u32` | Propbook underlying this market prices against. The market stores no oracle object IDs. |
| `expiry` | `u64` | Settlement timestamp in Unix milliseconds. |
| `cash` | `ExpiryCash` | DUSDC custody plus the isolated inventory-impact reserve. |
| `fee_incentive_balance` | `Balance<DUSDC>` | Sponsor-funded DUSDC available to subsidize taker fees on this expiry. |
| `strike_exposure` | `StrikeExposure` | Exposure lifecycle state across this expiry's strike ticks. |
| `ewma` | `EwmaState` | Smoothed gas-price statistics behind the congestion surcharge. |
| `mint_paused` | `bool` | When true, new mints abort. Every other flow stays available. |

The field order in the table is declaration order, which is also the BCS order of the object.

<details>
<summary>`ExpiryMarket` source</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	struct="ExpiryMarket"
/>

</details>

## Load a pricer {#load-a-pricer}

Every priced flow takes a `Pricer`, a snapshot of the oracle surface for one market. Build it with `load_live_pricer`, which reads the current canonical Propbook feeds for the market's underlying:

```move
public fun load_live_pricer(
    market: &ExpiryMarket,
    config: &ProtocolConfig,
    propbook_registry: &OracleRegistry,
    pyth: &PythFeed,
    bs_values: &BlockScholesValueStore,
    bs_svi: &BlockScholesSVIStore,
    clock: &Clock,
    ctx: &TxContext,
): Pricer
```

Three properties govern how you use the result:

- **`Pricer` has `copy` and `drop` but no `store`.** It cannot be saved in an object or carried between transactions, so every transaction that prices anything must call `load_live_pricer` first and consume the value in the same block.
- **A pricer is bound to one market.** It records `market.id()`, and any priced call that receives a pricer built for a different market aborts with `EWrongPricer`.
- **The load validates the oracle objects.** The four Propbook arguments are checked against the registry's current canonical bindings for the underlying, and a mismatch aborts inside `pricing`. The load also rejects an observation written earlier in the same transaction.

See [Oracle](/onchain-finance/deepbook/deepbook-predict/contract-information/oracle) for how to discover the `PythFeed`, `BlockScholesValueStore`, and `BlockScholesSVIStore` object IDs for an underlying.

<details>
<summary>`load_live_pricer` source</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="load_live_pricer"
	noComments
/>

</details>

## Quote a mint {#quote-a-mint}

Two quote functions price a prospective mint without changing market state. Both apply the live-mint and admission gates, so a quote that succeeds tells you the trade is admissible at the current oracle state:

```move
public fun quote_mint(
    market: &ExpiryMarket,
    config: &ProtocolConfig,
    pricer: &Pricer,
    lower_tick: u64,
    higher_tick: u64,
    max_premium: u64,
    min_quantity: u64,
    exact_quantity: bool,
    clock: &Clock,
    ctx: &mut TxContext,
): MintQuote

public fun quote_mint_for_account(
    market: &ExpiryMarket,
    wrapper: &AccountWrapper,
    config: &ProtocolConfig,
    pricer: &Pricer,
    lower_tick: u64,
    higher_tick: u64,
    max_premium: u64,
    min_quantity: u64,
    exact_quantity: bool,
    root: &AccumulatorRoot,
    clock: &Clock,
    ctx: &mut TxContext,
): MintQuote
```

The difference is whose terms the quote reflects. `quote_mint` prices an anonymous taker with no builder code. `quote_mint_for_account` reads the account's sticky builder code so the quoted builder fee matches what that account pays, and in budget mode it caps `max_premium` by the account's available DUSDC, including funds delivered through the accumulator but not yet settled.

Set `exact_quantity` to `true` to price a fixed size, in which case `min_quantity` carries the requested quantity. Set it to `false` to size a lot-rounded fill under the `max_premium` budget. Neither quote preflights the account balance, the slippage caps, or exposure-index capacity, and the congestion penalty it reports uses the pre-update congestion estimate, which is the same estimate a mint in the same state charges.

<details>
<summary>Quote source</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="quote_mint,quote_mint_for_account"
	noComments
/>

</details>

### The MintQuote struct {#mint-quote}

`MintQuote` has `copy` and `drop`, and it reports amounts in DUSDC base units except for `entry_probability`, which uses the 1e9 fixed-point scale. Read it through the nine getters, each of which takes `&MintQuote` and returns `u64`:

| **Getter** | **Meaning** |
|---|---|
| `quantity` | Sized position quantity, which is also the maximum payout. |
| `entry_probability` | Quoted per-contract range probability before fees, scaled by 1e9. |
| `premium` | Net premium, the contract's entry value. |
| `trading_fee` | Trading fee before the sponsor subsidy. |
| `fee_incentive_subsidy` | Sponsor-funded portion of the trading fee the trader does not pay. |
| `builder_fee` | Builder add-on, zero when the account carries no builder code. |
| `penalty_fee` | Congestion surcharge, zero unless gas is a high statistical outlier. |
| `inventory_impact_charge` | Isolated risk escrow charge, separate from every ordinary fee. |
| `all_in_cost` | Total DUSDC withdrawal from the account. |

`all_in_cost` is `premium + (trading_fee - fee_incentive_subsidy) + builder_fee + penalty_fee + inventory_impact_charge`. Pass that value, not `premium`, as the `max_cost` cap on the mint. The referral split does not appear in the quote, because it redistributes protocol proceeds after the trader's total is fixed rather than adding to it. See the [Design](/onchain-finance/deepbook/deepbook-predict/design) page for how each fee component is computed.

<details>
<summary>`MintQuote` and its getters</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	struct="MintQuote"
/>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="quantity,entry_probability,premium,trading_fee,fee_incentive_subsidy,builder_fee,penalty_fee,inventory_impact_charge,all_in_cost"
	noComments
/>

</details>

## Mint a position {#mint-a-position}

Two functions open a position. Both take the strike range as the tick pair `(lower_tick, higher_tick]`, both consume an account `Auth`, and both return the minted order ID as a `u256` that the transaction must consume:

```move
public fun mint_exact_quantity(
    market: &mut ExpiryMarket,
    wrapper: &mut AccountWrapper,
    auth: Auth,
    config: &ProtocolConfig,
    pricer: &Pricer,
    lower_tick: u64,
    higher_tick: u64,
    quantity: u64,
    max_cost: u64,
    max_probability: u64,
    root: &AccumulatorRoot,
    clock: &Clock,
    ctx: &mut TxContext,
): u256

public fun mint_exact_amount(
    market: &mut ExpiryMarket,
    wrapper: &mut AccountWrapper,
    auth: Auth,
    config: &ProtocolConfig,
    pricer: &Pricer,
    lower_tick: u64,
    higher_tick: u64,
    max_premium: u64,
    min_quantity: u64,
    max_cost: u64,
    root: &AccumulatorRoot,
    clock: &Clock,
    ctx: &mut TxContext,
): u256
```

`mint_exact_quantity` fixes the size. `max_cost` caps the all-in DUSDC withdrawal and `max_probability` caps the quoted per-contract probability before fees, and you can pass `std::u64::max_value!()` for either to leave it uncapped.

`mint_exact_amount` fixes the premium budget instead. The market first caps `max_premium` by the account's available DUSDC, computes the largest lot-rounded quantity whose premium fits, and aborts when that quantity falls below `min_quantity`. Fees, the builder add-on, and the congestion surcharge are charged on top of the budget, so `max_cost` is the only bound on the total withdrawal. `max_cost` is required here: zero aborts with `EMintCostCapRequired`, and no value disables it. This variant has no `max_probability` argument, because `min_quantity` against a fixed budget already bounds the price paid per contract.

Both calls run the same gates before anything moves. The running package version must be at or above the protocol version watermark, no pool valuation can be in progress, the pricer must be bound to this market, global trading must be enabled, this market's `mint_paused` flag must be false, and the expiry must hold enough cash to back the post-mint payout liability.

Positions are addressed by absolute integer ticks, with `0` as the open lower end and `1073741823` as the open higher end. A directional trade is a one-sided range: down is `(0, K]` and up is `(K, 1073741823]`. See [Market keys](/onchain-finance/deepbook/deepbook-predict/contract-information/market-keys) for the tick coordinate system and the sentinel values.

<details>
<summary>Mint source</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="mint_exact_quantity,mint_exact_amount"
	noComments
/>

</details>

The TypeScript SDK wraps both calls, resolves the market from an underlying and expiry, and converts raw strikes to ticks for you. `client.predict.tx.mint` builds `mint_exact_quantity` and `client.predict.tx.mintAmount` builds `mint_exact_amount`. Omitting the SDK's `maxCost` or `maxProbability` options sends `u64::MAX`, which is genuinely uncapped, so read a quote first and set a cap.

<details>
<summary>Quote and mint with the SDK</summary>

<ImportContent source="examples/deepbook-predict/src/mint.ts" mode="code" tag="mint-binary" />

</details>

## Redeem a position {#redeem-a-position}

Three functions close a position. Which one applies depends on whether the market has settled:

```move
public fun redeem_live(
    market: &mut ExpiryMarket,
    wrapper: &mut AccountWrapper,
    auth: Auth,
    config: &ProtocolConfig,
    pricer: &Pricer,
    order_id: u256,
    close_quantity: u64,
    min_probability: u64,
    min_proceeds: u64,
    root: &AccumulatorRoot,
    clock: &Clock,
    ctx: &mut TxContext,
): Option<u256>

public fun redeem_settled(
    market: &mut ExpiryMarket,
    wrapper: &mut AccountWrapper,
    auth: Auth,
    config: &ProtocolConfig,
    order_id: u256,
    root: &AccumulatorRoot,
    clock: &Clock,
    ctx: &mut TxContext,
)

public fun redeem_settled_permissionless(
    market: &mut ExpiryMarket,
    account_registry: &AccountRegistry,
    wrapper: &mut AccountWrapper,
    config: &ProtocolConfig,
    order_id: u256,
    root: &AccumulatorRoot,
    clock: &Clock,
    ctx: &mut TxContext,
)
```

`redeem_live` closes an unsettled position at the current range probability, in full or in part. A partial close removes the closed slice and returns a replacement order ID in the `Option<u256>`, which the transaction must consume; a full close returns `none`. `min_probability` floors the quoted per-contract probability and `min_proceeds` floors the net DUSDC credited after the trading fee, the builder fee, and the congestion surcharge, with inventory-impact rebate included. Pass `0` to disable either floor. A live redeem in the same `clock.timestamp_ms()` as the mint aborts with `EMintRedeemSameTimestamp`, which blocks an atomic mint, oracle update, and redeem inside one transaction.

`redeem_settled` closes a settled position for its terminal payout. A settled close is always full, so the function takes no quantity, and it runs no live pricing, so it takes no pricer. Payout is the full quantity when the settlement price lies inside `(lower_tick, higher_tick]` and zero otherwise.

`redeem_settled_permissionless` performs the same settled close without an account `Auth`. Instead of receiving authority from the caller, it mints Predict app-auth from the `AccountRegistry`, which is why it takes the registry as an argument and why anyone can run it on any holder's behalf. The proceeds always land in the position holder's account. An admin can turn the path off with `deauthorize_app<PredictApp>` on the account registry, and owners keep the `redeem_settled` path either way.

<details>
<summary>Redeem source</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="redeem_live,redeem_settled,redeem_settled_permissionless"
	noComments
/>

</details>

The SDK exposes `client.predict.tx.redeem` for the live close and `client.predict.tx.claimSettled` for the settled one. The facade sends `min_probability` and `min_proceeds` as `0`, so build the `moveCall` yourself when you need close-side slippage floors.

<details>
<summary>Close and claim with the SDK</summary>

<ImportContent source="examples/deepbook-predict/src/redeem.ts" mode="code" tag="redeem" />

</details>

## Settle a market {#settle-a-market}

`try_settle` is the single transition from live to settled:

```move
public fun try_settle(
    market: &mut ExpiryMarket,
    config: &ProtocolConfig,
    propbook_registry: &OracleRegistry,
    pyth: &PythFeed,
    bs_values: &BlockScholesValueStore,
    clock: &Clock,
): bool
```

Four properties matter when you compose it:

- **It is permissionless and idempotent.** No capability and no `Auth`. Calling it on an already settled market returns `true` without changing anything.
- **It returns `bool`, not an abort.** `true` means the market is now or was already settled, and `false` means settlement could not be recorded yet. The transaction must consume the return value.
- **It takes no SVI store.** Settlement reads an exact spot, not a volatility surface, so the argument list is the oracle registry, the Pyth feed, and the Block Scholes value store.
- **Exact Pyth first, Block Scholes after a grace period.** The call returns `false` before expiry. At or after expiry it tries the exact Pyth spot at the expiry timestamp. If Pyth is unavailable it returns `false` until 30 seconds past expiry, then tries the exact Block Scholes minute-boundary spot. If neither exact source is usable, the market stays unsettled and the call can be retried.

Settlement emits `MarketSettled` from `config_events`, carrying the settlement price and a `settlement_source` byte where `0` is Pyth and `1` is Block Scholes. It also releases the residual inventory-impact escrow, because no live close can follow.

Settled redeems, pool rebalancing, and pool valuation only read the current phase rather than driving the transition, so compose `try_settle` ahead of them in the same transaction whenever settlement might be due.

<details>
<summary>`try_settle` source</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="try_settle"
	noComments
/>

</details>

## Keeper and admin functions {#keeper-and-admin}

Two further functions change market state, and they differ in what they require:

| **Function** | **Gate** | **Effect** |
|---|---|---|
| `set_reference_tick` | Permissionless, version-gated, and blocked while a pool valuation is in progress. | Sets this expiry's reference fine-grid tick from the exact Propbook Pyth observation at `reference_tick_source_timestamp_ms`, floored to the market's `tick_size`. Returns the tick as a `u64`. |
| `set_mint_paused` | Requires `&AdminCap` and is version-gated. | Sets or clears `mint_paused`. A `PauseCap` holder can force the pause on one-way through the registry, which is not version-gated and cannot unpause. |

`set_reference_tick` is the keeper call that seeds a new market's anchor strike. It takes the oracle registry and the Pyth feed only, needing no Block Scholes objects and no pricer. The source observation must already be in the feed at the exact source timestamp, otherwise the call aborts with `EReferenceTickObservationMissing`. Until a market has a reference tick, `reference_tick` returns `none` and strike helpers that resolve against the reference cannot run.

<details>
<summary>Keeper and admin source</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="set_reference_tick,set_mint_paused"
	noComments
/>

</details>

## Read accessors {#read-accessors}

Every accessor below is permissionless and non-mutating, so a `devInspect` or simulated transaction reads them without gas. Grouped by what they tell you:

| **Group** | **Accessors** |
|---|---|
| Identity and phase | `id`, `propbook_underlying_id`, `expiry`, `is_settled`, `settlement_price`, `try_settlement_price`, `mint_paused` |
| Strike grid | `tick_size`, `admission_tick_size`, `reference_tick`, `reference_tick_source_timestamp_ms` |
| Cash and exposure | `cash_balance`, `inventory_impact_reserve`, `fee_incentive_balance`, `payout_liability`, `required_cash` |
| Frozen policy | `backing_buffer_lambda`, `expiry_fee_window_ms`, `expiry_fee_max_multiplier`, `inventory_impact_max_rate`, `inventory_impact_scale` |

Three of the accessors behave differently from the rest:

- **`settlement_price` aborts on an unsettled market** with `std::option::EOPTION_NOT_SET`, because it unwraps an empty option rather than running a phase check. Use `try_settlement_price`, which returns `Option<u64>`, when you do not already know the phase.
- **`reference_tick` returns `Option<u64>`** and is `none` until a keeper calls `set_reference_tick`.
- **`inventory_impact_scale` is the cadence allocation cap** that the registry froze into the market at creation, not a separately configured value.

Three more accessors need a market-bound `Pricer` or a settled market, because they mark positions rather than read stored fields:

```move
public fun current_nav(market: &ExpiryMarket, pricer: &Pricer): u64
public fun live_order_value(market: &ExpiryMarket, pricer: &Pricer, order_id: u256): u64
public fun settled_order_payout(market: &ExpiryMarket, order_id: u256): u64
```

`current_nav` is free expiry cash minus the marked liability of the exposure book, floored at zero. `live_order_value` is one order's full-close range value before fees. `settled_order_payout` is one order's terminal payout and aborts with `EMarketNotSettled` before settlement. None of the three proves that the caller holds `order_id`.

<details>
<summary>Read accessor source</summary>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="id,expiry,is_settled,try_settlement_price,cash_balance,required_cash,payout_liability,tick_size,admission_tick_size,reference_tick,mint_paused"
	noComments
/>

<ImportContent
	source="packages/predict/sources/expiry_market.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	fun="current_nav,live_order_value,settled_order_payout"
	noComments
/>

</details>

The SDK's `client.predict.read.markets`, `read.market`, and `read.pricer` cover the same ground over simulated transactions, with raw values converted to decimals. See the [Testnet tutorial](/onchain-finance/deepbook/deepbook-predict/tutorial) for the read path end to end.

## Error codes {#error-codes}

`expiry_market` declares ten abort codes. Resolve an abort against the module named in the abort location, because codes repeat across modules:

| **Code** | **Constant** | **Cause** |
|---|---|---|
| `0` | `EMintPaused` | This market's `mint_paused` flag is true. Other flows remain open. |
| `1` | `EMarketNotSettled` | A settled-only flow ran on an unsettled market, including `redeem_settled` and `settled_order_payout`. |
| `2` | `EMintCostAboveMax` | The quoted `all_in_cost` exceeds the `max_cost` you passed. |
| `3` | `EMintProbabilityAboveMax` | The quoted entry probability exceeds the `max_probability` you passed. |
| `4` | `EWrongPricer` | The pricer was built for a different market. Load one against this market in the same transaction. |
| `5` | `EReferenceTickObservationMissing` | No exact Pyth observation exists at the market's `reference_tick_source_timestamp_ms`. |
| `6` | `EMintRedeemSameTimestamp` | A live redeem ran in the same onchain millisecond the position was opened. |
| `7` | `ERedeemProbabilityBelowMin` | The quoted range probability fell below the `min_probability` floor. |
| `8` | `ERedeemProceedsBelowMin` | The net credit fell below the `min_proceeds` floor. |
| `9` | `EMintCostCapRequired` | `mint_exact_amount` was called with `max_cost` set to zero. |

Aborts from other modules reach these calls too. A pricer load can abort inside `pricing` when an oracle object does not match the registry binding or when a contributing observation was written in the same transaction. A gate can abort inside `protocol_config` with `ETradingPaused`, `EValuationInProgress`, `EPackageVersionDisabled`, or `EProtocolFrozen`. An account borrow can abort inside `account` with `EInvalidOwner` or `EBalanceTooLow`.

## Order events {#events}

Order-domain events live in `deepbook_predict::order_events` and all three carry `copy`, `drop`, and `store`. The field order below is declaration order, which is the order BCS decoding depends on. Do not reorder it.

### OrderMinted {#order-minted}

Emitted once per successful mint, from either mint function:

| **#** | **Field** | **Type** |
|---|---|---|
| 1 | `expiry_market_id` | `ID` |
| 2 | `account_id` | `ID` |
| 3 | `order_id` | `u256` |
| 4 | `position_root_id` | `u256` |
| 5 | `owner` | `address` |
| 6 | `lower_tick` | `u64` |
| 7 | `higher_tick` | `u64` |
| 8 | `entry_probability` | `u64` |
| 9 | `quantity` | `u64` |
| 10 | `premium` | `u64` |
| 11 | `trading_fee` | `u64` |
| 12 | `fee_incentive_subsidy` | `u64` |
| 13 | `builder_fee` | `u64` |
| 14 | `penalty_fee` | `u64` |
| 15 | `referral_fee` | `u64` |
| 16 | `inventory_impact_charge` | `u64` |
| 17 | `builder_code_id` | `Option<ID>` |
| 18 | `referrer_account_id` | `Option<ID>` |
| 19 | `onchain_timestamp_ms` | `u64` |
| 20 | `pyth_spot_source_timestamp_ms` | `u64` |
| 21 | `block_scholes_spot_source_timestamp_ms` | `u64` |
| 22 | `block_scholes_forward_source_timestamp_ms` | `u64` |
| 23 | `block_scholes_svi_source_timestamp_ms` | `u64` |

`referral_fee` appears here but not in `MintQuote`, because it redistributes protocol proceeds rather than adding to the trader's debit. The four source-timestamp fields are each pricing input's own economic clock, the timestamp its freshness was validated against.

### LiveOrderRedeemed {#live-order-redeemed}

Emitted once per `redeem_live`, whether the close is partial or full:

| **#** | **Field** | **Type** |
|---|---|---|
| 1 | `expiry_market_id` | `ID` |
| 2 | `account_id` | `ID` |
| 3 | `order_id` | `u256` |
| 4 | `position_root_id` | `u256` |
| 5 | `owner` | `address` |
| 6 | `quantity_closed` | `u64` |
| 7 | `remaining_quantity` | `u64` |
| 8 | `replacement_order_id` | `Option<u256>` |
| 9 | `redeem_amount` | `u64` |
| 10 | `trading_fee` | `u64` |
| 11 | `builder_fee` | `u64` |
| 12 | `penalty_fee` | `u64` |
| 13 | `inventory_impact_rebate` | `u64` |
| 14 | `builder_code_id` | `Option<ID>` |
| 15 | `onchain_timestamp_ms` | `u64` |
| 16 | `pyth_spot_source_timestamp_ms` | `u64` |
| 17 | `block_scholes_spot_source_timestamp_ms` | `u64` |
| 18 | `block_scholes_forward_source_timestamp_ms` | `u64` |
| 19 | `block_scholes_svi_source_timestamp_ms` | `u64` |

`replacement_order_id` is set on a partial close and absent on a full one. `position_root_id` stays constant across replacements, so join on it to follow one economic position through partial closes.

### SettledOrderRedeemed {#settled-order-redeemed}

Emitted by both `redeem_settled` and `redeem_settled_permissionless`:

| **#** | **Field** | **Type** |
|---|---|---|
| 1 | `expiry_market_id` | `ID` |
| 2 | `account_id` | `ID` |
| 3 | `order_id` | `u256` |
| 4 | `position_root_id` | `u256` |
| 5 | `owner` | `address` |
| 6 | `payout_amount` | `u64` |
| 7 | `onchain_timestamp_ms` | `u64` |

The event carries the payout but not the settlement price. Read that from the `MarketSettled` event or from `settlement_price` on the market.

<details>
<summary>Order event source</summary>

<ImportContent
	source="packages/predict/sources/events/order_events.move"
	mode="code"
	org="MystenLabs"
	repo="deepbookv3"
	branch="predict-testnet-8-21"
	struct="OrderMinted,LiveOrderRedeemed,SettledOrderRedeemed"
/>

</details>

Market lifecycle events such as `MarketCreated`, `ReferenceTickSet`, `MarketSettled`, and `ExpiryMarketMintPausedUpdated` live in `deepbook_predict::config_events`. See [Registry](/onchain-finance/deepbook/deepbook-predict/contract-information/registry) for those, and [Vault](/onchain-finance/deepbook/deepbook-predict/contract-information/vault) for the pool and liquidity events.
