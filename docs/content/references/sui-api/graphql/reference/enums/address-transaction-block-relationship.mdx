---
id: address-transaction-block-relationship
title: AddressTransactionBlockRelationship
hide_table_of_contents: false
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








The possible relationship types for a transaction block: sign, sent, received, or paid.


```graphql
enum AddressTransactionBlockRelationship {
  SIGN
  SENT
  RECV
  PAID
}
```




### Values

#### [<code style={{ fontWeight: 'normal' }}>AddressTransactionBlockRelationship.<b>SIGN</b></code>](#)  
> 
> 
> Transactions this address has signed either as a sender or as a sponsor
> 

#### [<code style={{ fontWeight: 'normal' }}>AddressTransactionBlockRelationship.<b>SENT</b></code>](#)  
> 
> 
> Transactions that transferred objects from this address
> 

#### [<code style={{ fontWeight: 'normal' }}>AddressTransactionBlockRelationship.<b>RECV</b></code>](#)  
> 
> 
> Transactions that sent objects to this addres
> 

#### [<code style={{ fontWeight: 'normal' }}>AddressTransactionBlockRelationship.<b>PAID</b></code>](#)  
> 
> 
> Transactions that were paid for by this address either as a sender or as a sponsor
> 



