---
id: shared-input
title: SharedInput
hide_table_of_contents: false
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








A Move object that's shared.


```graphql
type SharedInput {
  address: SuiAddress!
  initialSharedVersion: Int!
  mutable: Boolean!
}
```




### Fields

#### [<code style={{ fontWeight: 'normal' }}>SharedInput.<b>address</b></code>](#)<Bullet />[`SuiAddress!`](/references/sui-api/graphql/reference/scalars/sui-address) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/> 
> 
> 
> 
> 

#### [<code style={{ fontWeight: 'normal' }}>SharedInput.<b>initialSharedVersion</b></code>](#)<Bullet />[`Int!`](/references/sui-api/graphql/reference/scalars/int) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/> 
> 
> 
> The version that this this object was shared at.
> 

#### [<code style={{ fontWeight: 'normal' }}>SharedInput.<b>mutable</b></code>](#)<Bullet />[`Boolean!`](/references/sui-api/graphql/reference/scalars/boolean) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/> 
> 
> 
> Controls whether the transaction block can reference the shared object as a mutable
> reference or by value. This has implications for scheduling: Transactions that just read
> shared objects at a certain version (mutable = false) can be executed concurrently, while
> transactions that write shared objects (mutable = true) must be executed serially with
> respect to each other.
> 



### Implemented By

[`TransactionInput`](/references/sui-api/graphql/reference/unions/transaction-input)  <Badge class="badge badge--secondary" text="union"/>

