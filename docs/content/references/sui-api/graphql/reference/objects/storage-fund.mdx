---
id: storage-fund
title: StorageFund
hide_table_of_contents: false
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








SUI set aside to account for objects stored on-chain.


```graphql
type StorageFund {
  totalObjectStorageRebates: BigInt
  nonRefundableBalance: BigInt
}
```




### Fields

#### [<code style={{ fontWeight: 'normal' }}>StorageFund.<b>totalObjectStorageRebates</b></code>](#)<Bullet />[`BigInt`](/references/sui-api/graphql/reference/scalars/big-int) <Badge class="badge badge--secondary" text="scalar"/> 
> 
> 
> Sum of storage rebates of live objects on chain.
> 

#### [<code style={{ fontWeight: 'normal' }}>StorageFund.<b>nonRefundableBalance</b></code>](#)<Bullet />[`BigInt`](/references/sui-api/graphql/reference/scalars/big-int) <Badge class="badge badge--secondary" text="scalar"/> 
> 
> 
> The portion of the storage fund that will never be refunded through storage rebates.
> 
> The system maintains an invariant that the sum of all storage fees into the storage fund is
> equal to the sum of of all storage rebates out, the total storage rebates remaining, and the
> non-refundable balance.
> 



### Member Of

[`SuiSystemStateSummary`](/references/sui-api/graphql/reference/objects/sui-system-state-summary)  <Badge class="badge badge--secondary" text="object"/>

