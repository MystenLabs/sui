[package]
name = "turnip-town"
version = "0.0.1"

[dependencies]
Kiosk = { local = "../../../kiosk" }
Sui = { local = "../../../crates/sui-framework/packages/sui-framework" }

[addresses]
turnip_town = "0x0"
