// after Pop of the intermediate, the surviving field ref must still extend local#x

//# publish
mvir 0x42::m {
    public struct S has drop { f: u64 }

    fun t() {
        let x: Self::S;
        let r: &mut Self::S;
        let fr: &mut u64;
    label b0:
        x = S { f: 1 };
        r = &mut x;
        fr = &mut copy(r).S::f;
        _ = move(r);
        x = S { f: 2 };
        *move(fr) = 5;
        return;
    }
}
