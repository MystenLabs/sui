//# publish
mvir 0x42::A {
    public struct Coin has store { value: u64 }
    public struct T { f: Self::Coin }

    public fun t(this: &mut Self::T, y: Self::T) {
    label b0:
        *move(this) = move(y); // cannot assign/mutate without drop ability
        return;
    }
}
