//# publish

module 0x2.vector_ops_mixed_borrow {

foo(m: &mut u8) { label  l0: return; }

public test(v: &mut vector<u8>) {
    let m: &mut u8;
    let i: &u8;
label l0:
    m = vec_mut_borrow<u8>(copy(v), 0);
    i = vec_imm_borrow<u8>(move(v), 0);
    Self.foo(move(m));
    _ = *move(i);
    return;

}

}
