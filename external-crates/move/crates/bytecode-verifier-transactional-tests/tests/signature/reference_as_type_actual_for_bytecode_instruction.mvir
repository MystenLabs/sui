//# publish
mvir 0x1::M {
    fun id<T>(x: T) { label b0: return move(x); }

    fun foo() {
        let x: u64;
        let y: &u64;
    label b0:
        y = &x;
        // cannot use refs in generics
        Self::id<&u64>(move(y));
        return;
    }
}
