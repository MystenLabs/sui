//# publish
mvir 0x6::Test {
    public fun baz(k: u64, l: u64, m: u64) : u64 {
        let z: u64;
    label b0:
        z =  move(k) + move(l) + move(m);
        return move(z);
    }
}

//# run
mvir 0x42::m {
import 0x6::Test;

entry fun foo() {
label b0:
    // negative stack
    Test::baz();
    return;
}
}
