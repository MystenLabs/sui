//# publish
mvir 0x6::A {
    public fun push_u64(): u64  {
    label b0:
        return 42;
    }

    public fun push_bool(): bool {
    label b0:
        return true;
    }

    public fun foo(u: u64): u64 {
    label b0:
        return move(u) * 2;
    }

    public fun eat_stack(b: bool, u: u64) {
    label b0:
        return;
    }
}

//# run
mvir 0x42::m {
import 0x6::A;

entry fun foo() {
    let ans: u64;
label b0:
    (A::push_bool(), A::push_u64());
    ans = (A::foo());
    // negative stack
    (A::eat_stack());
    return;
}
}
