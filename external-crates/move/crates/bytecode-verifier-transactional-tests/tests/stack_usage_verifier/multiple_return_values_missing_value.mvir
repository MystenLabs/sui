//# publish
mvir 0x6::Test {
    public struct T { b: bool }

    public fun new(): Self::T {
    label b0:
        return T { b: true };
    }

    public fun test(i: &u64, x: Self::T): u64 * Self::T * bool {
    label b0:
        // too few values on stack for return, negative stack size
        return *move(i), move(x);
    }

    public fun destroy(x: Self::T) {
        let b: bool;
    label b0:
        T { b } = move(x);
        return;
    }
}
