//# publish
mvir 0x6::A {
    fun three(): u64 * u64 * u64 {
    label b0:
        return 0, 1, 2;
    }

    fun pop() {
    label b0:
        // positive stack size, missing pop
        _, _ = Self::three();
        return;
    }
}
