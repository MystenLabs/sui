//# publish

mvir 0x1::M {
    public struct X has drop { v: u64 }
    fun id<T>(x: &T): &T {
    label b0:
        return move(x);
    }

    fun t(x: Self::X): u64 {
    label b0:
        return *(&Self::id<Self::X>(&x).X::v);
    }
}
