//# publish
mvir 0x42::Test {
    public enum T{V { fr: bool} }

    public fun new(): Self::T {
    label b0:
        return T::V{fr: false};
    }

    public fun no(this: &mut Self::T) {
        let x: &mut bool;
    label b0:
        &mut T::V {fr: x} = move(this);
        // type mismatch on assignment/mutation
        *move(x) = 0;
        return;
    }
}

//# publish
mvir 0x42::Test {
    public enum T<B> {V { fr: B } }

    public fun new(): Self::T<bool> {
    label b0:
        return T::V<bool> {fr: false};
    }

    public fun no(this: &mut Self::T<bool>) {
        let x: &mut bool;
    label b0:
        &mut T::V<bool> {fr: x} = move(this);
        // type mismatch on assignment/mutation
        *move(x) = 0;
        return;
    }
}
