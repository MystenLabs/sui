//# publish
mvir 0x2::ByValue {
    public enum Foo { V { x: u64 } }

    fun foo(y: Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        Foo::V<u64> { x }  = move(y);
        return;
    }
}

//# publish
mvir 0x2::ByImmRef {
    public enum Foo { V { x: u64 } }

    fun foo(y: Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        &Foo::V<u64> { x } = move(y);
        return;
    }
}

//# publish
mvir 0x2::ByMutRef {
    public enum Foo { V { x: u64 } }

    fun foo(y: Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        &mut Foo::V<u64> { x } = move(y);
        return;
    }
}

//# publish
mvir 0x2::ByValue {
    public enum Foo { V { x: u64 } }

    fun foo(y: &Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        Foo::V<u64> { x } = move(y);
        return;
    }
}

//# publish
mvir 0x2::ByImmRef {
    public enum Foo { V { x: u64 } }

    fun foo(y: &Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        &Foo::V<u64> { x } = move(y);
        return;
    }
}

//# publish
mvir 0x2::ByMutRef {
    public enum Foo { V { x: u64 } }

    fun foo(y: &Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        &mut Foo::V<u64> { x } = move(y);
        return;
    }
}

//# publish
mvir 0x2::ByValue {
    public enum Foo { V { x: u64 } }

    fun foo(y: &mut Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        Foo::V<u64> { x } = move(y);
        return;
    }
}

//# publish
mvir 0x2::ByImmRef {
    public enum Foo { V { x: u64 } }

    fun foo(y: &mut Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        &Foo::V<u64> { x } = move(y);
        return;
    }
}

//# publish
mvir 0x2::ByMutRef {
    public enum Foo { V { x: u64 } }

    fun foo(y: &mut Self::Foo) {
        let x: u64;
    label b0:
        // invalid
        &mut Foo::V<u64> { x } = move(y);
        return;
    }
}
