# TODO: pkg-alt this needs to be fixed after modes are implemented DVX-1586
[package]
name = "build_include_exclude_stdlib"
edition = "2024.beta"

[addresses]
std = "0x1"

[dev-dependencies]
MoveStdlib = { local = "../../../../move-stdlib" }
