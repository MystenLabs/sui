module a::m {

fun one(): u64 {
    1
}

fun t(): u64 {
    let x = 1;
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    let x = if (x < 0) { x } else { one() };
    if (x > 10) { x } else { x }
}

}
