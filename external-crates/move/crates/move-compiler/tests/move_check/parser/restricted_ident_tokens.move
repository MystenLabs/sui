module a::example {
    fun examples(): u64 {
        let `abort` = 0;
        let `acquires` = 0;
        let `as` = 0;
        let `break` = 0;
        let `const` = 0;
        let `continue` = 0;
        let `copy` = 0;
        let `else` = 0;
        let `false` = 0;
        let `fun` = 0;
        let `friend` = 0;
        let `if` = 0;
        let `invariant` = 0;
        let `let` = 0;
        let `loop` = 0;
        let `module` = 0;
        let `move` = 0;
        let `native` = 0;
        let `public` = 0;
        let `return` = 0;
        let `script` = 0;
        let `spec` = 0;
        let `struct` = 0;
        let `true` = 0;
        let `use` = 0;
        let `while` = 0;
        `abort` +
        `acquires` +
        `as` +
        `break` +
        `const` +
        `continue` +
        `copy` +
        `else` +
        `false` +
        `fun` +
        `friend` +
        `if` +
        `invariant` +
        `let` +
        `loop` +
        `module` +
        `move` +
        `native` +
        `public` +
        `return` +
        `script` +
        `spec` +
        `struct` +
        `true` +
        `use` +
        `while`
    }
}
