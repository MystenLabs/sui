# Broken external resolver produces no output

[package]
name = "example"
edition = "2025"
license = "Apache-2.0"
authors = ["Move Team"]

[environments]
mainnet = "35834a8a"

[dependencies.mock.r.mock-resolver]
stdout = ""
stderr = "bad output"
