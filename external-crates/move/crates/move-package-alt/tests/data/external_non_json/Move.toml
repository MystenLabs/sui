# Broken external resolver produces non-toml output

[package]
name = "example"
edition = "2024"
license = "Apache-2.0"
authors = ["Move Team"]

[environments]
mainnet = "35834a8a"

[dependencies.mock.r.mock-resolver]
stdout = "this isn't toml"
stderr = "stderr output"
