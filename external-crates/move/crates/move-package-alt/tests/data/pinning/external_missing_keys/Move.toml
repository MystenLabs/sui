# Broken external resolver is missing output

[package]
name = "example"
edition = "2024.beta"
license = "Apache-2.0"
authors = ["Move Team"]

[environments]
mainnet = "35834a8a"

[dependencies.missing_env.r.mock-resolver]
stdout = """
# missing a response for the mainnet environment
[responses."(None, missing_env)"]
resolved = { local = "." }
warnings = []
"""
stderr = "stderr output"
