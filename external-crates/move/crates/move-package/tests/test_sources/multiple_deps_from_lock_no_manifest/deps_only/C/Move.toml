# This manifest file is largely empty on purpose to make sure that the main dependency graph (with actual
# dependencies) can simply be read from the lock file.

[package]
name = "C"

[addresses]
C = "0x0"
