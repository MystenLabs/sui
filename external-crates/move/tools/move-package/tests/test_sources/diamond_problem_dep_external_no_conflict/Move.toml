[package]
name = "Root"
version = "0.0.0"

[dependencies]
B = { local = "./deps_only/B" }

[dependencies.A]
resolver = "../resolvers/successful.sh"

[dependencies.A.packages]
Contains = "Anything"
Has = { No = "Schema" }
