# This manifest file is largely empty on purpose to make sure that the dependency graph (with actual
# dependencies) can simply be read from the lock file (that vis that, Move.lock and Move.locked file
# in the test directory are the same)
[package]
name = "test"
version = "0.0.0"

[addresses]
A = "0x3"

[dependencies]
C = { local = "./deps_only/C" }
D = { local = "./deps_only/D" }
