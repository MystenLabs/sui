apiVersion: v1
kind: Service
metadata:
  name: {{ include "indexer-writer.fullname" . }}
  labels:
    {{- include "indexer-writer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "indexer-writer.selectorLabels" . | nindent 4 }}
