apiVersion: v1
kind: Service
metadata:
  name: {{ include "indexer-reader.fullname" . }}
  labels:
    {{- include "indexer-reader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "indexer-reader.selectorLabels" . | nindent 4 }}
