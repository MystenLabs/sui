apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "indexer-reader.fullname" . }}-test-connection"
  labels:
    {{- include "indexer-reader.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "indexer-reader.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
