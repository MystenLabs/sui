apiVersion: v1
kind: Service
metadata:
  name: {{ include "sui-node.fullname" . }}
  labels:
    {{- include "sui-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sui-node.selectorLabels" . | nindent 4 }}
