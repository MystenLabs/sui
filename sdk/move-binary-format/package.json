{
  "name": "move-binary-format-tests",
  "version": "0.1.0",
  "description": "Move Binary Format implementation in WASM",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "wasm-pack build --target web --scope mysten",
    "build:dev": "wasm-pack build --target nodejs --debug --scope mysten",
    "build:release": "wasm-pack build --target web --release --scope mysten",
    "test": "vitest run"
  },
  "author": "Mysten Labs <build@mystenlabs.com>",
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.0.4",
    "vitest": "^0.30.1",
    "tsup": "^6.7.0"
  }
}
