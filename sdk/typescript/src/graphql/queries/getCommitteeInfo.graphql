# Copyright (c) Mysten Labs, Inc.
# SPDX-License-Identifier: Apache-2.0

query getCommitteeInfo($epochId: Int) {
	epoch(id: $epochId) {
		epochId
		validatorSet {
			activeValidators {
				credentials {
					protocolPubKey
				}
				votingPower
			}
		}
	}
}
